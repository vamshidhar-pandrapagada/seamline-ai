# shop: Seamline demo project

Three services with a planted contract mismatch on `order.created`
(orders emits `amount_cents`; payments will read `amount`).

```bash
cd examples/shop
seamline init --yes
```

`seamline.toml` and `.seamline/` are generated by `init` and not committed here.
